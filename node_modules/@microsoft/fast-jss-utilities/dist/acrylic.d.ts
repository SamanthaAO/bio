import { CSSRules } from "@microsoft/fast-jss-manager";
export interface AcrylicConfig {
    textureImage?: string;
    backgroundColor: string;
    fallbackBackgroundColor: string;
    blurRadius?: string;
    saturation?: string;
}
export declare const backdropFilterSupport: boolean;
export declare function applyAcrylic<T>(config: AcrylicConfig): CSSRules<T>;
