/**
 * @deprecated - use `classNames` utility from @microsoft/fast-web-utilites
 */
export function joinClasses(condition, existingClasses, newClass) {
    return condition ? `${existingClasses} ${newClass}` : existingClasses;
}
