import manageJss from "@microsoft/fast-jss-manager-react";
import { SelectStyles } from "@microsoft/fast-components-styles-msft";
import MSFTSelect from "./select";
import selectSchema from "./select.schema";
/*
 * The type returned by manageJss type is very complicated so we'll let the
 * compiler infer the type instead of re-declaring just for the package export
 */
/* tslint:disable-next-line:typedef */
const Select = manageJss(SelectStyles)(MSFTSelect);
export { Select, selectSchema, };
