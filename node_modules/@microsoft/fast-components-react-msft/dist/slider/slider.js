import Foundation from "@microsoft/fast-components-foundation-react";
import { Slider as BaseSlider } from "@microsoft/fast-components-react-base";
import { DesignSystemConsumer } from "@microsoft/fast-jss-manager-react";
import React from "react";
import { DisplayNamePrefix } from "../utilities";
class Slider extends Foundation {
    constructor() {
        super(...arguments);
        this.handledProps = {
            managedClasses: void 0,
        };
    }
    /**
     * Renders the component
     */
    render() {
        return (React.createElement(DesignSystemConsumer, null, (designSystem) => {
            let designSystemDirection;
            if (designSystem !== undefined &&
                designSystem.direction !== undefined) {
                designSystemDirection = designSystem.direction;
            }
            return (React.createElement(BaseSlider, Object.assign({}, this.unhandledProps(), { dir: designSystemDirection || null, managedClasses: this.props.managedClasses }), this.props.children));
        }));
    }
}
Slider.displayName = `${DisplayNamePrefix}Slider`;
export default Slider;
